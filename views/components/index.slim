main
    #content
        .articles
            - for doc in @docs
                article
                    .preview
                        img src="/img/#{doc[:preview]}" alt="Doc Post"
                    .title
                        - if @current_user.logged_in?()
                            - if doc[:users].include?(@current_user.data[:id])
                                form action="/document/#{doc[:id]}" method="post"
                                    button type="submit" = doc[:title]